version: '3'

tasks:
  dependencies-up:
    aliases: [du]
    desc: Start docker compose dependencies
    cmds:
      - docker compose -f ./docker-compose.yml up -d

  dependencies-down:
    aliases: [dd]
    desc: Start docker compose dependencies
    cmds:
      - docker compose -f ./docker-compose.yml down

  test:
    desc: "Run all the tests in google module"
    deps:
      - dependencies-up
    cmds:
      - go test ./...
